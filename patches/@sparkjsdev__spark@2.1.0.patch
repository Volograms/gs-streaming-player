diff --git a/dist/spark.cjs.js b/dist/spark.cjs.js
index 9f85cfa400493f0c0bd6cb256026be1caf2a9476..3ebb522e9883d5f1d3cdde117a57039f75074ea1 100644
--- a/dist/spark.cjs.js
+++ b/dist/spark.cjs.js
@@ -10292,28 +10292,44 @@ const _SparkRenderer = class _SparkRenderer extends THREE__namespace.Mesh {
     const ordering = new Uint32Array(this.maxSplats);
     const readback = Readback.ensureBuffer(maxSplats, this.readback32);
     this.readback32 = readback;
-    await this.readbackDepth({
-      current,
-      renderer: this.renderer,
+    const readbackStartedAt = performance.now();
+    const providedSortKeys = await this.sortKeyProvider?.({
+      maxSplats,
       numSplats,
-      readback
+      readback,
+      sortRadial: this.sortRadial ?? true,
+      viewDirection: current.viewDirection,
+      viewOrigin: current.viewOrigin
     });
+    const readbackSource = providedSortKeys === true ? "cpu" : "gpu";
+    if (!providedSortKeys) {
+      await this.readbackDepth({
+        current,
+        renderer: this.renderer,
+        numSplats,
+        readback
+      });
+    }
+    const readbackDurationMs = performance.now() - readbackStartedAt;
     if (this.sortPause > 0) {
       await new Promise((resolve) => setTimeout(resolve, this.sortPause));
     }
     if (!this.sortWorker) {
       this.sortWorker = new SplatWorker();
     }
+    const workerSortStartedAt = performance.now();
     const result = await this.sortWorker.call("sortSplats32", {
       numSplats,
       readback,
       ordering
     });
+    const workerSortDurationMs = performance.now() - workerSortStartedAt;
     if (this.sortDelay > 0) {
       await new Promise((resolve) => setTimeout(resolve, this.sortDelay));
     }
     this.readback32 = result.readback;
     this.activeSplats = result.activeSplats;
+    const orderingUploadStartedAt = performance.now();
     if (this.orderingTexture) {
       if (rows > this.orderingTexture.image.height) {
         this.orderingTexture.dispose();
@@ -10361,6 +10377,16 @@ const _SparkRenderer = class _SparkRenderer extends THREE__namespace.Mesh {
         renderer.state.bindTexture(gl.TEXTURE_2D, null);
       }
     }
+    const orderingUploadDurationMs = performance.now() - orderingUploadStartedAt;
+    this.onSortTiming?.({
+      atMs: performance.now(),
+      numSplats,
+      orderingUploadDurationMs,
+      readbackDurationMs,
+      readbackSource,
+      totalDurationMs: performance.now() - now,
+      workerSortDurationMs
+    });
     if (this.current.mappingVersion === current.mappingVersion) {
       if (this.current.mappingVersion !== this.display.mappingVersion) {
         this.accumulators.push(this.display);
@@ -10506,7 +10532,18 @@ const _SparkRenderer = class _SparkRenderer extends THREE__namespace.Mesh {
       if (this.lodUpdates.length > 0) {
         const lodUpdates = this.lodUpdates;
         this.lodUpdates = [];
+        const treeUpdateStartedAt = performance.now();
         await worker.call("updateLodTrees", { ranges: lodUpdates });
+        const treeUpdateDurationMs = performance.now() - treeUpdateStartedAt;
+        for (const update of lodUpdates) {
+          const splats = this.lodIdToSplats.get(update.lodId);
+          if (splats instanceof PagedSplats) {
+            splats.reportPreparation({
+              durationMs: treeUpdateDurationMs,
+              phase: "tree-update"
+            });
+          }
+        }
         this.lodDirty = true;
       }
       if (this.lodDirty) {
@@ -10548,9 +10585,14 @@ const _SparkRenderer = class _SparkRenderer extends THREE__namespace.Mesh {
       this.lodIds.set(splats, { lodId, lastTouched: performance.now() });
       this.lodIdToSplats.set(lodId, splats);
     } else {
+      const registrationStartedAt = performance.now();
       const { lodId } = await worker.call("newSharedLodTree", {
         lodId: this.pagerId
       });
+      splats.reportPreparation({
+        durationMs: performance.now() - registrationStartedAt,
+        phase: "tree-registration"
+      });
       this.lodIds.set(splats, { lodId, lastTouched: performance.now() });
       this.lodIdToSplats.set(lodId, splats);
     }
@@ -10601,6 +10643,17 @@ const _SparkRenderer = class _SparkRenderer extends THREE__namespace.Mesh {
       instances
     });
     this.lastTraverseTime = performance.now() - traverseStart;
+    const traversedSplats = new Set();
+    for (const instance of Object.values(instances)) {
+      const splats = this.lodIdToSplats.get(instance.lodId);
+      if (splats instanceof PagedSplats && !traversedSplats.has(splats)) {
+        traversedSplats.add(splats);
+        splats.reportPreparation({
+          durationMs: this.lastTraverseTime,
+          phase: "tree-traversal"
+        });
+      }
+    }
     const { keyIndices, chunks, pixelLimit } = result;
     this.lastPixelLimit = pixelLimit;
     const totalLodSplats = Object.values(keyIndices).reduce(
@@ -11101,6 +11154,7 @@ class PagedSplats {
   constructor(options) {
     var _a2;
     this.pager = options.pager;
+    this.onPreparation = options.onPreparation;
     this.rootUrl = options.rootUrl ?? "";
     this.requestHeader = options.requestHeader;
     this.withCredentials = options.withCredentials;
@@ -11143,6 +11197,12 @@ class PagedSplats {
   setMaxSh(maxSh) {
     this.maxSh = maxSh;
   }
+  reportPreparation(event) {
+    this.onPreparation == null ? void 0 : this.onPreparation({
+      ...event,
+      atMs: performance.now()
+    });
+  }
   getRadMeta() {
     if (this.radMetaPromise) {
       return this.radMetaPromise;
@@ -11185,6 +11245,7 @@ class PagedSplats {
     return this.rootUrl.replace(/-lod-0\./, `-lod-${chunk}.`);
   }
   async fetchDecodeChunk(chunk) {
+    const fetchStartedAt = performance.now();
     let decodeBytes = void 0;
     if (this.fileType === SplatFileType.RAD) {
       const { meta, chunksStart } = await this.getRadMeta();
@@ -11246,7 +11307,13 @@ class PagedSplats {
     } else {
       throw new Error("No url or fileBytes provided");
     }
-    return await workerPool.withWorker(async (worker) => {
+    this.reportPreparation({
+      chunk,
+      durationMs: performance.now() - fetchStartedAt,
+      phase: "chunk-fetch"
+    });
+    const decodeStartedAt = performance.now();
+    const decoded = await workerPool.withWorker(async (worker) => {
       var _a2, _b2, _c, _d, _e;
       if (!this.pager) {
         throw new Error("PagedSplats.pager not set");
@@ -11299,6 +11366,12 @@ class PagedSplats {
       this.sh3Codes = lodSplats.extra.sh3Codes ?? this.sh3Codes;
       return lodSplats;
     });
+    this.reportPreparation({
+      chunk,
+      durationMs: performance.now() - decodeStartedAt,
+      phase: "chunk-decode"
+    });
+    return decoded;
   }
   update(numSplats, indices) {
     if (!this.pager) {
@@ -11462,6 +11535,7 @@ const _SplatPager = class _SplatPager {
     this.fetchers = [];
     this.fetched = [];
     this.fetchPriority = [];
+    this.requestedChunks = [];
     this.packedTexture = new DynoUsampler2DArray({
       value: this.newUint32ArrayTexture(
         new Uint32Array(this.maxPages * 256 * 256 * 4),
@@ -11802,6 +11876,48 @@ const _SplatPager = class _SplatPager {
     }
     return chunks[chunk];
   }
+  prepareChunk(splats, chunk = 0, options = {}) {
+    const existing = this.getSplatsChunk(splats, chunk);
+    const uploadPending = (page) => this.newUploads.some(({ page: queuedPage }) => queuedPage === page) || this.readyUploads.some(({ page: queuedPage }) => queuedPage === page);
+    if (existing && !uploadPending(existing.page)) {
+      return Promise.resolve({ chunk, page: existing.page, reusedPage: true });
+    }
+    if (!this.requestedChunks.some(({ splats: queuedSplats, chunk: queuedChunk }) => queuedSplats === splats && queuedChunk === chunk)) {
+      this.requestedChunks.push({ splats, chunk });
+    }
+    return new Promise((resolve, reject) => {
+      let timer;
+      const cleanup = () => {
+        if (timer !== void 0) clearTimeout(timer);
+        options.signal == null ? void 0 : options.signal.removeEventListener("abort", abort);
+      };
+      const removeRequest = () => {
+        this.requestedChunks = this.requestedChunks.filter(({ splats: queuedSplats, chunk: queuedChunk }) => queuedSplats !== splats || queuedChunk !== chunk);
+      };
+      const abort = () => {
+        cleanup();
+        removeRequest();
+        reject(new DOMException("Chunk preparation was aborted", "AbortError"));
+      };
+      const check = () => {
+        if ((options.signal == null ? void 0 : options.signal.aborted) === true) {
+          abort();
+          return;
+        }
+        const mapping = this.getSplatsChunk(splats, chunk);
+        if (mapping && !uploadPending(mapping.page)) {
+          cleanup();
+          removeRequest();
+          resolve({ chunk, page: mapping.page, reusedPage: false });
+          return;
+        }
+        this.driveFetchers();
+        timer = setTimeout(check, 16);
+      };
+      options.signal == null ? void 0 : options.signal.addEventListener("abort", abort, { once: true });
+      check();
+    });
+  }
   insertSplatsChunkPage(splats, chunk, page, now) {
     if (!this.splatsChunkToPage.has(splats)) {
       this.splatsChunkToPage.set(splats, []);
@@ -11922,7 +12038,11 @@ const _SplatPager = class _SplatPager {
     const needed = [];
     const overflow = [];
     let numPages = 0;
-    for (const { splats, chunk } of this.fetchPriority) {
+    const priorities = [
+      ...this.requestedChunks,
+      ...this.fetchPriority.filter(({ splats, chunk }) => !this.requestedChunks.some(({ splats: requestedSplats, chunk: requestedChunk }) => requestedSplats === splats && requestedChunk === chunk))
+    ];
+    for (const { splats, chunk } of priorities) {
       const pageLru = this.getSplatsChunk(splats, chunk);
       if (pageLru) {
         if (numPages >= this.maxPages) {
@@ -12013,14 +12133,24 @@ const _SplatPager = class _SplatPager {
         break;
       }
       const { splats, chunk, data } = fetched;
+      const allocationStartedAt = performance.now();
       let page = this.allocatePage();
+      let reusedPage = false;
       if (page === void 0) {
         page = this.allocateFreeable();
+        reusedPage = page !== void 0;
         if (page === void 0) {
           return;
         }
       }
       this.insertSplatsChunkPage(splats, chunk, page, now);
+      splats.reportPreparation({
+        chunk,
+        durationMs: performance.now() - allocationStartedAt,
+        page,
+        phase: "page-allocation",
+        reusedPage
+      });
       const { numSplats, extra } = data;
       this.lodTreeUpdates.push({
         splats,
@@ -12047,7 +12177,17 @@ const _SplatPager = class _SplatPager {
         break;
       }
       const { page, numSplats, packedArray, extArray, extra } = upload;
+      const uploadStartedAt = performance.now();
       this.uploadPage(page, packedArray, extra, extArray);
+      const mapping = this.pageToSplatsChunk[page];
+      if (mapping) {
+        mapping.splats.reportPreparation({
+          chunk: mapping.chunk,
+          durationMs: performance.now() - uploadStartedAt,
+          page,
+          phase: "gpu-upload"
+        });
+      }
     }
   }
   consumeLodTreeUpdates() {
diff --git a/dist/spark.module.js b/dist/spark.module.js
index aec3d5590a50f27b56a39169c08b4557afe5748c..1cdfe4e788cd7b14da05782fc9456f93bbfc392f 100644
--- a/dist/spark.module.js
+++ b/dist/spark.module.js
@@ -10273,28 +10273,44 @@ const _SparkRenderer = class _SparkRenderer extends THREE.Mesh {
     const ordering = new Uint32Array(this.maxSplats);
     const readback = Readback.ensureBuffer(maxSplats, this.readback32);
     this.readback32 = readback;
-    await this.readbackDepth({
-      current,
-      renderer: this.renderer,
+    const readbackStartedAt = performance.now();
+    const providedSortKeys = await this.sortKeyProvider?.({
+      maxSplats,
       numSplats,
-      readback
+      readback,
+      sortRadial: this.sortRadial ?? true,
+      viewDirection: current.viewDirection,
+      viewOrigin: current.viewOrigin
     });
+    const readbackSource = providedSortKeys === true ? "cpu" : "gpu";
+    if (!providedSortKeys) {
+      await this.readbackDepth({
+        current,
+        renderer: this.renderer,
+        numSplats,
+        readback
+      });
+    }
+    const readbackDurationMs = performance.now() - readbackStartedAt;
     if (this.sortPause > 0) {
       await new Promise((resolve) => setTimeout(resolve, this.sortPause));
     }
     if (!this.sortWorker) {
       this.sortWorker = new SplatWorker();
     }
+    const workerSortStartedAt = performance.now();
     const result = await this.sortWorker.call("sortSplats32", {
       numSplats,
       readback,
       ordering
     });
+    const workerSortDurationMs = performance.now() - workerSortStartedAt;
     if (this.sortDelay > 0) {
       await new Promise((resolve) => setTimeout(resolve, this.sortDelay));
     }
     this.readback32 = result.readback;
     this.activeSplats = result.activeSplats;
+    const orderingUploadStartedAt = performance.now();
     if (this.orderingTexture) {
       if (rows > this.orderingTexture.image.height) {
         this.orderingTexture.dispose();
@@ -10342,6 +10358,16 @@ const _SparkRenderer = class _SparkRenderer extends THREE.Mesh {
         renderer.state.bindTexture(gl.TEXTURE_2D, null);
       }
     }
+    const orderingUploadDurationMs = performance.now() - orderingUploadStartedAt;
+    this.onSortTiming?.({
+      atMs: performance.now(),
+      numSplats,
+      orderingUploadDurationMs,
+      readbackDurationMs,
+      readbackSource,
+      totalDurationMs: performance.now() - now,
+      workerSortDurationMs
+    });
     if (this.current.mappingVersion === current.mappingVersion) {
       if (this.current.mappingVersion !== this.display.mappingVersion) {
         this.accumulators.push(this.display);
@@ -10487,7 +10513,18 @@ const _SparkRenderer = class _SparkRenderer extends THREE.Mesh {
       if (this.lodUpdates.length > 0) {
         const lodUpdates = this.lodUpdates;
         this.lodUpdates = [];
+        const treeUpdateStartedAt = performance.now();
         await worker.call("updateLodTrees", { ranges: lodUpdates });
+        const treeUpdateDurationMs = performance.now() - treeUpdateStartedAt;
+        for (const update of lodUpdates) {
+          const splats = this.lodIdToSplats.get(update.lodId);
+          if (splats instanceof PagedSplats) {
+            splats.reportPreparation({
+              durationMs: treeUpdateDurationMs,
+              phase: "tree-update"
+            });
+          }
+        }
         this.lodDirty = true;
       }
       if (this.lodDirty) {
@@ -10529,9 +10566,14 @@ const _SparkRenderer = class _SparkRenderer extends THREE.Mesh {
       this.lodIds.set(splats, { lodId, lastTouched: performance.now() });
       this.lodIdToSplats.set(lodId, splats);
     } else {
+      const registrationStartedAt = performance.now();
       const { lodId } = await worker.call("newSharedLodTree", {
         lodId: this.pagerId
       });
+      splats.reportPreparation({
+        durationMs: performance.now() - registrationStartedAt,
+        phase: "tree-registration"
+      });
       this.lodIds.set(splats, { lodId, lastTouched: performance.now() });
       this.lodIdToSplats.set(lodId, splats);
     }
@@ -10582,6 +10624,17 @@ const _SparkRenderer = class _SparkRenderer extends THREE.Mesh {
       instances
     });
     this.lastTraverseTime = performance.now() - traverseStart;
+    const traversedSplats = new Set();
+    for (const instance of Object.values(instances)) {
+      const splats = this.lodIdToSplats.get(instance.lodId);
+      if (splats instanceof PagedSplats && !traversedSplats.has(splats)) {
+        traversedSplats.add(splats);
+        splats.reportPreparation({
+          durationMs: this.lastTraverseTime,
+          phase: "tree-traversal"
+        });
+      }
+    }
     const { keyIndices, chunks, pixelLimit } = result;
     this.lastPixelLimit = pixelLimit;
     const totalLodSplats = Object.values(keyIndices).reduce(
@@ -11082,6 +11135,7 @@ class PagedSplats {
   constructor(options) {
     var _a2;
     this.pager = options.pager;
+    this.onPreparation = options.onPreparation;
     this.rootUrl = options.rootUrl ?? "";
     this.requestHeader = options.requestHeader;
     this.withCredentials = options.withCredentials;
@@ -11124,6 +11178,12 @@ class PagedSplats {
   setMaxSh(maxSh) {
     this.maxSh = maxSh;
   }
+  reportPreparation(event) {
+    this.onPreparation == null ? void 0 : this.onPreparation({
+      ...event,
+      atMs: performance.now()
+    });
+  }
   getRadMeta() {
     if (this.radMetaPromise) {
       return this.radMetaPromise;
@@ -11166,6 +11226,7 @@ class PagedSplats {
     return this.rootUrl.replace(/-lod-0\./, `-lod-${chunk}.`);
   }
   async fetchDecodeChunk(chunk) {
+    const fetchStartedAt = performance.now();
     let decodeBytes = void 0;
     if (this.fileType === SplatFileType.RAD) {
       const { meta, chunksStart } = await this.getRadMeta();
@@ -11227,7 +11288,13 @@ class PagedSplats {
     } else {
       throw new Error("No url or fileBytes provided");
     }
-    return await workerPool.withWorker(async (worker) => {
+    this.reportPreparation({
+      chunk,
+      durationMs: performance.now() - fetchStartedAt,
+      phase: "chunk-fetch"
+    });
+    const decodeStartedAt = performance.now();
+    const decoded = await workerPool.withWorker(async (worker) => {
       var _a2, _b2, _c, _d, _e;
       if (!this.pager) {
         throw new Error("PagedSplats.pager not set");
@@ -11280,6 +11347,12 @@ class PagedSplats {
       this.sh3Codes = lodSplats.extra.sh3Codes ?? this.sh3Codes;
       return lodSplats;
     });
+    this.reportPreparation({
+      chunk,
+      durationMs: performance.now() - decodeStartedAt,
+      phase: "chunk-decode"
+    });
+    return decoded;
   }
   update(numSplats, indices) {
     if (!this.pager) {
@@ -11443,6 +11516,7 @@ const _SplatPager = class _SplatPager {
     this.fetchers = [];
     this.fetched = [];
     this.fetchPriority = [];
+    this.requestedChunks = [];
     this.packedTexture = new DynoUsampler2DArray({
       value: this.newUint32ArrayTexture(
         new Uint32Array(this.maxPages * 256 * 256 * 4),
@@ -11783,6 +11857,48 @@ const _SplatPager = class _SplatPager {
     }
     return chunks[chunk];
   }
+  prepareChunk(splats, chunk = 0, options = {}) {
+    const existing = this.getSplatsChunk(splats, chunk);
+    const uploadPending = (page) => this.newUploads.some(({ page: queuedPage }) => queuedPage === page) || this.readyUploads.some(({ page: queuedPage }) => queuedPage === page);
+    if (existing && !uploadPending(existing.page)) {
+      return Promise.resolve({ chunk, page: existing.page, reusedPage: true });
+    }
+    if (!this.requestedChunks.some(({ splats: queuedSplats, chunk: queuedChunk }) => queuedSplats === splats && queuedChunk === chunk)) {
+      this.requestedChunks.push({ splats, chunk });
+    }
+    return new Promise((resolve, reject) => {
+      let timer;
+      const cleanup = () => {
+        if (timer !== void 0) clearTimeout(timer);
+        options.signal == null ? void 0 : options.signal.removeEventListener("abort", abort);
+      };
+      const removeRequest = () => {
+        this.requestedChunks = this.requestedChunks.filter(({ splats: queuedSplats, chunk: queuedChunk }) => queuedSplats !== splats || queuedChunk !== chunk);
+      };
+      const abort = () => {
+        cleanup();
+        removeRequest();
+        reject(new DOMException("Chunk preparation was aborted", "AbortError"));
+      };
+      const check = () => {
+        if ((options.signal == null ? void 0 : options.signal.aborted) === true) {
+          abort();
+          return;
+        }
+        const mapping = this.getSplatsChunk(splats, chunk);
+        if (mapping && !uploadPending(mapping.page)) {
+          cleanup();
+          removeRequest();
+          resolve({ chunk, page: mapping.page, reusedPage: false });
+          return;
+        }
+        this.driveFetchers();
+        timer = setTimeout(check, 16);
+      };
+      options.signal == null ? void 0 : options.signal.addEventListener("abort", abort, { once: true });
+      check();
+    });
+  }
   insertSplatsChunkPage(splats, chunk, page, now) {
     if (!this.splatsChunkToPage.has(splats)) {
       this.splatsChunkToPage.set(splats, []);
@@ -11903,7 +12019,11 @@ const _SplatPager = class _SplatPager {
     const needed = [];
     const overflow = [];
     let numPages = 0;
-    for (const { splats, chunk } of this.fetchPriority) {
+    const priorities = [
+      ...this.requestedChunks,
+      ...this.fetchPriority.filter(({ splats, chunk }) => !this.requestedChunks.some(({ splats: requestedSplats, chunk: requestedChunk }) => requestedSplats === splats && requestedChunk === chunk))
+    ];
+    for (const { splats, chunk } of priorities) {
       const pageLru = this.getSplatsChunk(splats, chunk);
       if (pageLru) {
         if (numPages >= this.maxPages) {
@@ -11994,14 +12114,24 @@ const _SplatPager = class _SplatPager {
         break;
       }
       const { splats, chunk, data } = fetched;
+      const allocationStartedAt = performance.now();
       let page = this.allocatePage();
+      let reusedPage = false;
       if (page === void 0) {
         page = this.allocateFreeable();
+        reusedPage = page !== void 0;
         if (page === void 0) {
           return;
         }
       }
       this.insertSplatsChunkPage(splats, chunk, page, now);
+      splats.reportPreparation({
+        chunk,
+        durationMs: performance.now() - allocationStartedAt,
+        page,
+        phase: "page-allocation",
+        reusedPage
+      });
       const { numSplats, extra } = data;
       this.lodTreeUpdates.push({
         splats,
@@ -12028,7 +12158,17 @@ const _SplatPager = class _SplatPager {
         break;
       }
       const { page, numSplats, packedArray, extArray, extra } = upload;
+      const uploadStartedAt = performance.now();
       this.uploadPage(page, packedArray, extra, extArray);
+      const mapping = this.pageToSplatsChunk[page];
+      if (mapping) {
+        mapping.splats.reportPreparation({
+          chunk: mapping.chunk,
+          durationMs: performance.now() - uploadStartedAt,
+          page,
+          phase: "gpu-upload"
+        });
+      }
     }
   }
   consumeLodTreeUpdates() {
diff --git a/dist/types/SparkRenderer.d.ts b/dist/types/SparkRenderer.d.ts
index e0505656c42c1e07eb07255f0eafd17656eb0e97..8b84e436f1c41babc4779ec0d59be1656acea06f 100644
--- a/dist/types/SparkRenderer.d.ts
+++ b/dist/types/SparkRenderer.d.ts
@@ -327,6 +327,23 @@ export declare class SparkRenderer extends THREE.Mesh {
     lastFrame: number;
     updateTimeoutId: number;
     onDirty?: () => void;
+    sortKeyProvider?: (request: {
+        maxSplats: number;
+        numSplats: number;
+        readback: Uint32Array;
+        sortRadial: boolean;
+        viewDirection: THREE.Vector3;
+        viewOrigin: THREE.Vector3;
+    }) => boolean | Promise<boolean>;
+    onSortTiming?: (sample: {
+        atMs: number;
+        numSplats: number;
+        orderingUploadDurationMs: number;
+        readbackDurationMs: number;
+        readbackSource: "cpu" | "gpu";
+        totalDurationMs: number;
+        workerSortDurationMs: number;
+    }) => void;
     dirty: boolean;
     orderingTexture: THREE.DataTexture | null;
     maxSplats: number;
diff --git a/dist/types/SplatPager.d.ts b/dist/types/SplatPager.d.ts
index 968e4534933abc3772056fd21068eb5ff76b0f67..bb6f041c7c32d634f2f79df66c924a0e62e0e014 100644
--- a/dist/types/SplatPager.d.ts
+++ b/dist/types/SplatPager.d.ts
@@ -2,6 +2,16 @@ import { dyno } from '.';
 import { SplatSource } from './SplatMesh';
 import { ExtResult, PackedResult, RadMeta, SplatEncoding, SplatFileType } from './defines';
 import * as THREE from "three";
+export type PagedSplatsPreparationPhase = "chunk-fetch" | "chunk-decode" | "page-allocation" | "gpu-upload" | "tree-registration" | "tree-update" | "tree-traversal";
+export interface PagedSplatsPreparationEvent {
+    atMs: number;
+    chunk?: number;
+    durationMs: number;
+    page?: number;
+    phase: PagedSplatsPreparationPhase;
+    reusedPage?: boolean;
+}
+export type PagedSplatsPreparationCallback = (event: Readonly<PagedSplatsPreparationEvent>) => void;
 export interface PagedSplatsOptions {
     pager?: SplatPager;
     rootUrl?: string;
@@ -10,9 +20,11 @@ export interface PagedSplatsOptions {
     fileBytes?: Uint8Array;
     fileType?: SplatFileType;
     maxSh?: number;
+    onPreparation?: PagedSplatsPreparationCallback;
 }
 export declare class PagedSplats implements SplatSource {
     pager?: SplatPager;
+    onPreparation: PagedSplatsPreparationCallback | undefined;
     rootUrl: string;
     requestHeader?: Record<string, string>;
     withCredentials?: boolean;
@@ -38,6 +50,7 @@ export declare class PagedSplats implements SplatSource {
     constructor(options: PagedSplatsOptions);
     dispose(): void;
     setMaxSh(maxSh: number): void;
+    reportPreparation(event: Omit<PagedSplatsPreparationEvent, "atMs">): void;
     getRadMeta(): Promise<{
         meta: RadMeta;
         chunksStart: number;
@@ -86,6 +99,14 @@ export interface SplatPagerOptions {
      */
     numFetchers?: number;
 }
+export interface SplatPagerPrepareChunkOptions {
+    signal?: AbortSignal;
+}
+export interface SplatPagerPreparedChunk {
+    chunk: number;
+    page: number;
+    reusedPage: boolean;
+}
 export declare class SplatPager {
     renderer: THREE.WebGLRenderer;
     extSplats: boolean;
@@ -147,6 +168,10 @@ export declare class SplatPager {
         splats: PagedSplats;
         chunk: number;
     }[];
+    requestedChunks: {
+        splats: PagedSplats;
+        chunk: number;
+    }[];
     packedTexture: dyno.DynoUsampler2DArray<"packedTexture", THREE.DataArrayTexture>;
     extTexture: dyno.DynoUsampler2DArray<"extTexture", THREE.DataArrayTexture>;
     sh1Texture: dyno.DynoUsampler2DArray<"sh1", THREE.DataArrayTexture>;
@@ -198,6 +223,7 @@ export declare class SplatPager {
         page: number;
         lru: number;
     } | undefined;
+    prepareChunk(splats: PagedSplats, chunk?: number, options?: SplatPagerPrepareChunkOptions): Promise<SplatPagerPreparedChunk>;
     private insertSplatsChunkPage;
     private removeSplatsChunkPage;
     private uploadPage;
